{
  "name": "yourivalkyra-website",
  "private": true,
  "type": "module",
  "scripts": {
    "images": "node scripts/optimize-images.mjs",
    "build": "npm run images && node scripts/build-site.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "sharp": "^0.34.3",
    "wrangler": "^4.27.0"
  }
}
